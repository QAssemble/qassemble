import string as string
import matplotlib as mat
import re as re
import matplotlib as mpl
import matplotlib.pyplot as plt
import numpy as np
from pylab import cm
import matplotlib.font_manager as fm
from collections import OrderedDict
import json, os, shutil
import itertools
from scipy.fftpack import fftn, ifftn


import DiagE


nomega=1000

beta=1/(8.617333262145e-5*300)

omega=np.pi/beta*(2*np.arange(nomega)+1)*1j


fomega=1.0/(omega-10)

moment=DiagE.diagefourier.gmoment5(fomega,omega)
print(moment)
print(DiagE.diagecommon.ifactorial(0))
print(DiagE.diagecommon.ifactorial(1))
print(DiagE.diagecommon.ifactorial(2))
print(DiagE.diagecommon.ifactorial(3))

# moment=np.zeros(3)
# moment[0]=1.0
# moment[1]=(fomega[-1]+np.conj(fomega[-1]))/2*(omega[-1])**2
# moment[2]=(fomega[-1]-np.conj(fomega[-1])-1*2/omega[-1])/2*(omega[-1])**3
# print(moment)

# fomega_tail=(moment[0]/omega+moment[1]/omega**2+moment[2]/omega**3)

# fomega_new=fomega-fomega_tail

# # fomega=1.0/(omega-10.0)

# moment=np.array([1.0, 10.0, 100.0])

ntau=10000

tau=np.zeros(ntau+1)


# for itau in range(ntau+1):
#     if (itau < ntau//2+1):
#         tau[itau]=(-np.log(ntau/2+1-itau)+np.log(ntau/2+1))/np.log(ntau/2+1)*beta/2.0
#     else:
#         tau[itau]=(beta-tau[ntau-itau])


for itau in range(ntau+1):
    if (itau <= ntau//2):
        tau[itau]=(itau/(ntau//2))**2/2.0*beta
    else:
        tau[itau]=(beta-tau[ntau-itau])

# # tau=np.arange(ntau+1)/ntau*beta
        
# plt.close(11);plt.figure(11);plt.plot(tau, 'r.-')
# # ftau=DiagE.diagefourier.fermionomegatotau(omega,fomega_new,[0.0, 0.0, 0.0],tau)
ftau=DiagE.diagefourier.fermionomegatotau5(omega,fomega,moment,tau)

# # plt.close(11);plt.figure(11);plt.plot(tau, np.real(ftau), 'r.',tau, np.imag(ftau), 'b.' )
# # plt.close(12);plt.figure(12);plt.plot(np.diff(tau)[0:200])

fomega_out=DiagE.diagefourier.tautofreq(tau, ftau, omega)

fomega2=fomega_out

# # plt.close(1);plt.figure(1);plt.plot(tau, np.real(ftau), 'r.',tau, np.imag(ftau), 'b.' )
# # plt.close(12);plt.figure(12);plt.plot(tau, (np.real(ftau)+1/2)/0.5, 'b.' )
plt.close(2);plt.figure(2);plt.plot(np.imag(omega), np.real(fomega), 'r.', np.imag(omega), np.real(fomega2), 'b.');plt.xlim([0, 100]);plt.grid('on')
plt.close(3);plt.figure(3);plt.plot(np.imag(omega), np.imag(fomega), 'r.', np.imag(omega), np.imag(fomega2), 'b.');plt.xlim([0, 100]);plt.grid('on')
plt.close(4);plt.figure(4);plt.plot(np.imag(omega), np.imag(fomega-fomega2)/abs(fomega), 'r.-', np.imag(omega), np.real(fomega-fomega2)/abs(fomega), 'b.-')



# # plt.close(20);plt.figure(20);plt.plot(np.imag(omega), np.real(1.0/(omega-10.0)**2), 'r.-', np.imag(omega), np.imag(1.0/(omega-10.0)**2), 'b.-');plt.xlim([0, 100]);plt.grid('on')
plt.show()
